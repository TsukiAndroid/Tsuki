<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".main.ui.MainActivity">

        <!-- Activity-level background: extends full-screen behind AppBar/SearchBar -->
        <ImageView
                android:id="@+id/activity_bg_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:alpha="0"
                android:visibility="gone" />

        <!-- Subtle dim over the full-screen background for readability -->
        <View
                android:id="@+id/activity_bg_dim"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#66000000"
                android:alpha="0"
                android:visibility="gone" />

        <androidx.fragment.app.FragmentContainerView
                android:id="@id/container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior"
                tools:layout="@layout/fragment_list" />


        <com.google.android.material.appbar.AppBarLayout
                android:id="@+id/appbar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@null"
                android:clipToPadding="false"
                android:elevation="0dp"
                android:fitsSystemWindows="false"
                android:stateListAnimator="@null"
                app:elevation="0dp"
                app:liftOnScroll="false"
                app:liftOnScrollColor="@null">

                <io.github.landwarderer.futon.core.ui.widgets.WindowInsetHolder
                        android:id="@+id/insetsHolder"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_gravity="top"
                        android:fitsSystemWindows="true"
                        app:layout_scrollFlags="scroll|enterAlways|snap" />

                <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:paddingStart="24dp"
                        android:paddingEnd="16dp"
                        android:paddingTop="6dp"
                        android:paddingBottom="2dp"
                        android:gravity="center_vertical"
                        app:layout_scrollFlags="scroll|enterAlways">

                        <TextView
                                android:layout_width="0dp"
                                android:layout_height="wrap_content"
                                android:layout_weight="1"
                                android:text="Tsuki"
                                android:textAppearance="?attr/textAppearanceHeadlineLarge"
                                android:textColor="?attr/colorOnBackground" />

                        <ImageButton
                                android:id="@+id/btn_downloads"
                                android:layout_width="40dp"
                                android:layout_height="40dp"
                                android:layout_marginEnd="8dp"
                                android:background="@drawable/bg_icon_circle_glass"
                                android:contentDescription="@string/downloads"
                                android:scaleType="center"
                                android:src="@drawable/ic_download"
                                android:tint="@android:color/white" />

                        <ImageButton
                                android:id="@+id/btn_incognito"
                                android:layout_width="40dp"
                                android:layout_height="40dp"
                                android:layout_marginEnd="8dp"
                                android:alpha="0.55"
                                android:background="@drawable/bg_icon_circle_glass"
                                android:contentDescription="@string/incognito_mode"
                                android:scaleType="center"
                                android:src="@drawable/ic_incognito"
                                android:tint="@android:color/white" />

                        <ImageButton
                                android:id="@+id/btn_settings"
                                android:layout_width="40dp"
                                android:layout_height="40dp"
                                android:background="@drawable/bg_icon_circle_glass"
                                android:contentDescription="@string/settings"
                                android:scaleType="center"
                                android:src="@drawable/ic_settings"
                                android:tint="@android:color/white" />

                </LinearLayout>

                <com.google.android.material.search.SearchBar
                        android:id="@+id/search_bar"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="12dp"
                        android:layout_marginEnd="12dp"
                        android:layout_marginBottom="6dp"
                        android:hint="@string/search_manga"
                        app:forceDefaultNavigationOnClickListener="true"
                        app:layout_scrollFlags="scroll|enterAlways|snap" />

        </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.search.SearchView
                android:id="@+id/search_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:hint="@string/search_hint"
                app:layout_anchor="@id/search_bar">

                <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/recyclerView_search"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:clipToPadding="false"
                        android:orientation="vertical"
                        android:scrollbars="vertical"
                        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

        </com.google.android.material.search.SearchView>

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
                android:id="@+id/fab"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:elevation="10dp"
                android:text="@string/_continue"
                android:visibility="gone"
                app:backgroundTint="@color/glass_teal"
                app:icon="@drawable/ic_read"
                app:layout_anchor="@id/bottomNav"
                app:layout_anchorGravity="top|end"
                app:layout_behavior="io.github.landwarderer.futon.main.ui.MainActionButtonBehavior"
                app:layout_dodgeInsetEdges="bottom"
                app:layout_insetEdge="bottom"
                tools:ignore="InconsistentLayout"
                tools:visibility="visible" />

        <!--
          Nav bar blur view — sits at the same position and size as the pill nav.
          Starts invisible; shown only when nav bar blur is enabled and intensity > 0.
          Its translationY is kept in sync with bottomNav in MainActivity.
        -->
        <io.github.landwarderer.futon.core.ui.widgets.BlurBehindView
                android:id="@+id/navBlurView"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_gravity="bottom"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_marginBottom="12dp"
                android:background="@android:color/transparent"
                android:visibility="invisible" />

        <io.github.landwarderer.futon.core.ui.widgets.SlidingBottomNavigationView
                android:id="@+id/bottomNav"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/bg_bottom_nav_pill"
                android:clipToOutline="true"
                android:fitsSystemWindows="false" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
